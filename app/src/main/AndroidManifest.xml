<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.workhome">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WorkHome">
        <activity
            android:name=".fragment.IntelbohAmdLohActivity"
            android:exported="false" />
        <activity
            android:name=".fragment.AddTaskActivity"
            android:exported="false" />
        <activity
            android:name=".fragment.TiarActivity"
            android:exported="false" />
        <activity
            android:name=".fragment.BasicActivity"
            android:exported="false" />
        <activity
            android:name=".fragment.Other"
            android:exported="false" />
        <activity
            android:name=".fragment.ViewPager"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.WorkHome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>